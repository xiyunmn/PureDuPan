# 本项目工作约定

- Windows 命令优先使用 PowerShell 7（`pwsh.exe -NoLogo -NoProfile`）；文件默认按 UTF-8 处理。
- 禁止使用 Frida，包括启动/附加、server、Gadget 以及以其为后端的工具。用户明确要求避免应用检测、设备负面影响及其他应用风控。
- 实机动态诊断通过本项目 LSPosed 模块增加临时、窄范围插桩。测试日志只写宿主私有目录，通过 root ADB 读取；禁止使用通用 LSPosed 日志通道输出测试日志。
- 测试前保留原模块 APK 和用户设置；结束后移除临时插桩、测试开关及设备端临时组件，恢复测试改动的用户设置，并将最新修复后的无插桩 APK 安装到连接设备，作为最终留装版本。除非用户明确要求回滚，不再恢复旧模块。不得清除宿主数据。
